import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Net worth / Patrimonio",
};

export default function WealthLayout({ children }: { children: React.ReactNode }) {
  return children;
}
