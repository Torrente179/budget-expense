# Keep only the Apple mockup

## Summary

Removed every other design mockup. The only remaining visual proposal is `mockups/cupertino/`.

## Product Changes

- Deleted `mockups/app`, `graphite`, `maison`, `north`, `pavilion`, `range`, `revolut`, `up`, and `up-redesign`.
- Deleted `premium-redesign/` (design spec and all HTML demos).
- Production Next.js app is unchanged.

## Data Model

None.

## Validation

`mockups/` contains only `cupertino/`. Open http://127.0.0.1:8774/index.html.
