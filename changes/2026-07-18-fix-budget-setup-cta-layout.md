## Summary

Fixed the first-run “Three things to get started” checklist so desktop CTAs no longer float to the far right as oversized, mismatched pills.

## Product Changes

- Setup step actions sit under each step’s title and caption instead of in a stretched right column.
- Compact, consistent `sm` triggers for plan / methods / create (short labels, shared sizing).
- Method CTA uses the standard outline style in setup (no special info-blue treatment).

## Validation

- Visually inspect Budget → empty-state setup card on desktop: CTAs align with copy, same height as “Crear”.
- Confirm each button still opens its sheet (monthly plan, methods, create objective).
- Confirm mobile still works and header actions outside setup are unchanged.
